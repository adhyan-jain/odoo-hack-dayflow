export default function SignUpPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="w-full max-w-sm space-y-6">
        <h1 className="text-2xl font-semibold">Create your Dayflow account</h1>
        {/* TODO: employee ID / email / password / role form using createClient() from @/lib/supabase/client */}
      </div>
    </main>
  );
}
